name: Build MAUI App

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 10.0.x

      - name: Install MAUI workloads
        run: dotnet workload install maui

      - name: Build Windows
        run: dotnet build -c Release -f net10.0-windows10.0.19041.0 --no-dependencies

      - name: Build Android
        run: dotnet build -c Release -f net10.0-android --no-dependencies

  macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 10.0.x

      - name: Install MAUI workloads
        run: dotnet workload install maui

      - name: Build MacCatalyst
        run: dotnet build -c Release -f net9.0-maccatalyst --no-dependencies

      - name: Build iOS
        run: dotnet build -c Release -f net9.0-ios --no-dependencies
